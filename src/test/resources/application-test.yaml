spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      timeout: 2000

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

internal:
  api:
    token: test-internal-token
